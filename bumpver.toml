[bumpver]
current_version = "v2023.330.2"
version_pattern = "vYYYY.MM0D.PATCH"
commit_message = "bump version {old_version} -> {new_version}"
commit = true
tag = true
push = false

[bumpver.file_patterns]
"src/dendropy/__init__.py" = [
    "{pep440_version}",
    "{pep440_version}",
]
"bumpver.toml" = [
    'current_version = "{version}"',
]
